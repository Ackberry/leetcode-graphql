## This project is to access leetcode's api and query it for getfolk.app . 

Run the server with `go run .`. It defaults to port `8080` when `PORT` is unset or empty.

To use a different port:

```sh
PORT=3000 go run .
```
